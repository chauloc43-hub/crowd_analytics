# Local application runtime. The Modal image installs only the production
# subset at deploy/requirements-runtime.txt.
-r deploy/requirements-runtime.txt
# The Windows app uses this only when a system FFmpeg binary is unavailable.
imageio-ffmpeg==0.6.0
